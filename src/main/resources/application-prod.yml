spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/daily_plan_project
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics